<% provide :title, 'Home' %>

<h1>Wicked Science News</h1>

<h2>A user contributed collection of current interesting science stories.</h2>

<table>
  <tr>
    <td>Title</td>
    <td>Author</td>
    <td>Link</td>
    <td></td>
    <td></td>
  </tr>
  <% @articles.each do |article| %>
    <tr>
      <td><%= article.title %></td>
      <td><%= article.author %></td>
      <td><%= article.link %></td>
      <td><%= link_to 'Edit article', edit_article_path(article) %></td>
      <td><%= link_to 'Delete', article, :method => :delete, :data => { :confirm => 'Are you sure?' } %></td>
    </tr>
  <% end %>
</table>

<p><%= link_to 'Add article', new_article_path %></p>
